#include <stdio.h>
#include <string.h>
/**
 * main-A program that print a line;
 * Return: 0 (Success);
 */
int main(void)
{
char s[50];
strcpy(s, "\"Programming is like building a multilingual puzzle")
puts(s);
return (0);
}

